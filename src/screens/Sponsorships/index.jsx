import React from "react";

import { connect } from "react-redux";

const Sponsorships = () => {
  return <h1>Sponsorships</h1>;
};

const mapStateToProps = (state) => ({});

export default connect(mapStateToProps, {})(Sponsorships);
