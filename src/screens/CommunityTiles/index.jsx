import React from "react";

import { connect } from "react-redux";

const CommunityTiles = () => {
  return <h1>Community Tiles</h1>;
};

const mapStateToProps = (state) => ({});

export default connect(mapStateToProps, {})(CommunityTiles);
